import { RegisterPage } from "@/components/shared/AuthForms";

export const metadata = {
  title: "Create Account — NewsMind AI",
  description: "Join NewsMind AI — your AI-powered news intelligence platform. Free to join.",
};

export default function Register() {
  return <RegisterPage />;
}
